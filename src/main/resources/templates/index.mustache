<!DOCTYPE HTML>
<html>
<head>
    <title>스프링 부트 웹서비스</title>
    <meta http-equiv="Content-Type" content=Htext/html; charset="UTF-8" />
</head>
<body>
    {{! 20230208 Setting layout header }}
    {{>layout/header}}
    <hl>스프링 부트로 시작하는 웹 서비스</hl>

    {{! 20230208 게시판 글 등록 버튼 추가 }}
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>

            {{! userName이 존재한다면 아래 로직 수행 }}
            {{#userName}}
            Logged in as : <span id="user">{{userName}}</span>
            <a href="/logout">Logout</a>
            {{/userName}}

            {{! userName이 존재 하지 않는다면 아래 로직 수행 }}
            {{^userName}}
            <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a>
            <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a>
            {{/userName}}
        </div>
    </div>
    <br>
    {{! 20230211 Table layer }}
    <table class="table table-horizontal table-bordered">
        <thead>
            <tr>
                <th>게시글번호</th>
                <th>제목</th>
                <th>작성자</th>
                <th>최종수정일</th>
            </tr>
        </thead>
        <tbody>
            {{#posts}}
            <tr>
                <td>{{id}}</td>
                <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
            {{/posts}}
        </tbody>
    </table>

    {{! 20230208 Setting layout footer }}
    {{>layout/footer}}
</body>
</html>
